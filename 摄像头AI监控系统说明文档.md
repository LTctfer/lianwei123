# 摄像头AI监控系统说明文档

## 系统概述

本系统是一个基于AI视觉检测的环境监控系统，主要用于实时监测和识别环境污染违规行为。系统通过调用网络摄像头视频流，使用YOLO深度学习模型进行目标检测，自动识别裸土未覆盖、扬尘污染、车辆冒黑烟等环境违规行为，并将检测结果存储到数据库中。

## 系统架构

```
摄像头设备 → 视频流API → AI检测模型 → 数据库存储 → 报警处理
     ↓           ↓           ↓           ↓           ↓
  实时监控    流媒体服务    智能识别    结果存储    违规报警
```

## 核心功能

### 1. 多模型AI检测
- **裸土检测**: 识别未覆盖的裸土区域
- **扬尘检测**: 识别扬尘污染超标情况
- **黑烟检测**: 识别车辆冒黑烟违规行为

### 2. 智能资源管理
- **Token自动管理**: 自动获取和刷新API访问令牌
- **数据库连接优化**: 避免长时间连接超时
- **批处理机制**: 提高处理效率，减少资源消耗

### 3. 实时监控处理
- **24小时监控**: 在工作时间(6:00-22:00)自动运行
- **循环检测**: 批量处理所有摄像头，每小时一轮
- **异常恢复**: 完善的错误处理和自动重试机制

## 技术栈

### 核心依赖
- **Python 3.8+**: 主要编程语言
- **OpenCV (cv2)**: 图像处理和视频流处理
- **Ultralytics YOLO**: AI目标检测模型
- **MySQL Connector**: 数据库连接和操作
- **Requests**: HTTP API调用

### AI模型
- **luotu.pt**: 裸土检测模型
- **dust.pt**: 扬尘检测模型  
- **black_smoke.pt**: 黑烟检测模型

## 配置说明

### 数据库配置
```python
db_config = {
    'user': 'zjlianwei',           # 数据库用户名
    'password': 'Lianwei0907#',    # 数据库密码
    'host': '10.71.79.129',        # 数据库主机地址
    'database': 'color_warning',   # 数据库名称
    'port': 3306                   # 数据库端口
}
```

### API配置
```python
url_path = "http://10.71.79.129:9025/color/warning/"  # API基础路径
```

### 模型配置
```python
model_config = [
    {
        "model_path": "luotu.pt",      # 模型文件路径
        "class_id": 0,                 # 目标类别ID
        "alarm_info": "裸土未覆盖",     # 报警信息
        "alarm_type": "裸土"           # 报警类型
    },
    # ... 其他模型配置
]
```

## 数据库表结构

### 摄像头信息表 (tb_security_camera_info)
```sql
- id: 摄像头ID
- vr_id: 录像机ID
- channel_num: 通道号
- is_deleted: 是否删除 (0=未删除)
- is_alarm: 是否启用报警 (1=启用)
```

### 录像机信息表 (tb_video_recorder_info)
```sql
- id: 录像机ID
- ip: IP地址
- user: 用户名
- pwd: 密码
- port: 端口
```

### AI检测报警表 (tb_ai_camera_warn)
```sql
- camera_id: 摄像头ID
- alarm_info: 报警信息
- alarm_time: 报警时间
- alarm_image: 检测结果图片路径
```

## 核心流程

### 1. 系统启动流程
```
1. 初始化日志系统
2. 创建检测结果目录
3. 进入主循环
4. 检查当前时间是否在工作时间内
5. 获取摄像头列表
6. 开始批处理检测
```

### 2. 单次检测流程
```
1. 获取API Token
2. 获取摄像头视频流URL
3. 打开视频流并读取一帧图像
4. 使用三个AI模型进行检测
5. 如果检测到目标:
   - 绘制检测框
   - 保存检测结果图片
   - 写入数据库报警记录
6. 释放资源
```

### 3. 批处理机制
```
1. 每次处理10个摄像头
2. 为每批创建新的数据库连接
3. 循环处理所有摄像头
4. 每批完成后等待1小时
5. 继续下一批检测
```

## 关键类和函数

### TokenManager类
**功能**: 管理API访问令牌
- `get_valid_token()`: 获取有效Token，自动处理过期和刷新

### 核心函数
- `get_video_url()`: 获取摄像头视频流URL
- `process_camera_frame()`: 处理单个摄像头的检测
- `run_camera_check_batch()`: 批量处理摄像头检测
- `fetch_all_cameras()`: 获取最新摄像头列表
- `main()`: 主程序循环

## 运行参数

### 时间控制
- **工作时间**: 6:00 - 22:00
- **检测间隔**: 每小时一轮
- **批处理大小**: 每批10个摄像头
 /‘【= 
### 超时设置
- **视频流读取超时**: 15秒
- **API请求超时**: 10-15秒
- **错误重试间隔**: 60秒

## 部署说明

### 1. 环境准备
```bash
# 安装Python依赖
pip install opencv-python
pip install ultralytics
pip install mysql-connector-python
pip install requests

# 创建检测结果目录
mkdir -p detections
```

### 2. 模型文件
确保以下模型文件存在于程序目录:
- `luotu.pt` (裸土检测模型)
- `dust.pt` (扬尘检测模型)
- `black_smoke.pt` (黑烟检测模型)

### 3. 数据库准备
确保MySQL数据库已创建相关表结构，并配置正确的连接参数。

### 4. 启动程序
```bash
python camera_monitor.py
```

## 监控和维护

### 日志管理
- **日志文件**: `camera_monitor.log`
- **日志级别**: INFO
- **日志内容**: 包含检测过程、错误信息、统计数据

### 检测结果
- **图片保存路径**: `/lianwei/spec/sanseyj/detection/new/detections/`
- **文件命名格式**: `{camera_id}_{timestamp}.jpg`

### 性能监控
- 每批检测完成后输出处理时间和成功率
- 记录失败的摄像头和原因
- 统计整体检测效果

## 故障排除

### 常见问题
1. **数据库连接失败**: 检查网络连接和数据库配置
2. **Token获取失败**: 检查API服务状态和认证信息
3. **视频流无法访问**: 检查摄像头状态和网络连通性
4. **模型加载失败**: 检查模型文件是否存在和完整

### 错误处理机制
- 自动重试失败的操作
- 跳过有问题的摄像头继续处理其他摄像头
- 详细的错误日志记录
- 异常情况下的优雅降级

## 扩展功能

### 可扩展方向
1. **新增检测模型**: 添加更多环境违规行为检测
2. **实时报警**: 集成短信、邮件等报警通知
3. **Web管理界面**: 提供可视化的管理和监控界面
4. **数据分析**: 添加统计分析和趋势预测功能
5. **移动端支持**: 开发移动端监控应用

### 性能优化
1. **GPU加速**: 使用GPU进行模型推理加速
2. **多线程处理**: 并行处理多个摄像头
3. **缓存机制**: 优化频繁访问的数据
4. **负载均衡**: 分布式部署支持更多摄像头

## 技术支持

### 联系方式
- **开发团队**: 联维科技
- **技术支持**: 请查看日志文件进行问题诊断
- **更新维护**: 定期更新AI模型和系统功能

### 版本信息
- **当前版本**: v1.0
- **最后更新**: 2024年
- **兼容性**: Python 3.8+, MySQL 5.7+
